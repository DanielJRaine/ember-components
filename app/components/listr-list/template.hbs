{{yield}}
<div>
    <h4 class="list-header" {{action "toggleListDetail"}}> Title: {{list.title}} </h4>
    <ul>
        {{#each list.items as |item|}}
            {{listr-list/item content=item.content}}
        {{/each}}
    </ul>
</div>